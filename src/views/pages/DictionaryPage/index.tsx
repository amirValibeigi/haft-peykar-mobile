import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const DictionaryPage = () => {
  return (
    <SafeAreaView>
      <Text>DictionaryPage</Text>
    </SafeAreaView>
  );
};

export default React.memo(DictionaryPage);
