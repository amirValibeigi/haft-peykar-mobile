import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const HomePage = () => {
  return (
    <SafeAreaView>
      <Text>HomePage</Text>
    </SafeAreaView>
  );
};

export default React.memo(HomePage);
