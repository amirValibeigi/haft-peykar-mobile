import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const PodcastPage = () => {
  return (
    <SafeAreaView>
      <Text>PodcastPage</Text>
    </SafeAreaView>
  );
};

export default React.memo(PodcastPage);
